# --- Django ---
DEBUG=False
SECRET_KEY='django-insecure-f(qs5*dg-%ol+%5#t2s)shf)v#*r5v4hi9g#&#k*vqcbo)co$6'
ALLOWED_HOSTS=api.bodyremix.ir,127.0.0.1

# --- PostgreSQL (Use service name 'db') ---
POSTGRES_DB=fitness_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Mn958_zero
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- Redis (Use service name 'redis') ---
REDIS_URL=redis://redis:6379/0

# --- MinIO (Internal for Django) ---
MINIO_ROOT_USER=minioadminme
MINIO_ROOT_PASSWORD=minioadminme
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET_NAME=media

# --- External MinIO (For Flutter) ---
# Once HAProxy is set up, this could also be https://api.bodyremix.ir/minio
MINIO_EXTERNAL_ENDPOINT=http://api.bodyremix.ir:9000