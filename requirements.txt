# Core Django and Extensions
Django~=4.2
django-extensions~=3.2
django-filter~=23.2
django-timezone-field~=5.1

# REST Framework and API
djangorestframework~=3.14
djangorestframework-simplejwt~=5.3
drf-yasg~=1.21

# Storage and Media
# MinIO is S3-compatible, so django-storages + boto3 is all that's needed -
# settings.py uses storages.backends.s3boto3.S3Boto3Storage.
Pillow~=10.0
django-storages~=1.14
boto3~=1.43
whitenoise~=6.7

# PostgreSQL
psycopg2-binary~=2.9

# CORS and Environment Management
django-cors-headers~=4.2
python-decouple~=3.8

# ASGI stack
# Required by BOTH the chat websockets and the Telegram webhook - the app
# must be served as config.asgi:application, never config.wsgi.
channels~=4.0
channels-redis~=4.1
daphne~=4.0
uvicorn[standard]~=0.46
# Since uvicorn 0.30 the bundled uvicorn.workers module is deprecated in
# favour of this standalone package (gunicorn -k uvicorn_worker.UvicornWorker).
uvicorn-worker~=0.4
gunicorn~=25.3

# Telegram bot
# NOTE: the import name is `telegram`, but the PyPI package is
# `python-telegram-bot`. The unrelated `telegram` package on PyPI does NOT
# provide telegram.ext and will break the bot at import time.
python-telegram-bot~=22.8

# AI receipt review (optional - no-ops when ANTHROPIC_API_KEY is unset)
anthropic~=0.116.0

# Misc
requests~=2.31
google-auth~=2.25

# Payments
# Legacy: the VPN flow is manual card-to-card, so this is only still needed
# if the older Stripe-based payments app is in use.
stripe~=12.2

# Testing
pytest~=8.3
pytest-django~=4.6

# Translation (uncomment if you enable django-parler)
# django-parler~=2.3
# django-parler-rest~=2.0